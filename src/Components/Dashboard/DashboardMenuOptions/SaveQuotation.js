import React from 'react';
import QuotationFormat from '../../Quotation/QuotationFormat';

const SaveQuotation = ({ item }) => {
    return <QuotationFormat item={item} showSave={true} />;
};

export default SaveQuotation;